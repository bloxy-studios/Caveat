# Trained artifacts (models.joblib, metrics.json, meta.json, feature_spec.json,
# sample_genomes.json) are generated by `python -m caveat_ml.train` and are NOT
# committed. This file just keeps the directory present on a fresh clone.
